name: react

on:
  push:
    branches: ["**"]
    paths:
      - "packages/ethds-react/**"
      - "packages/ethds-tokens/**"
      - ".github/workflows/react.yml"
  pull_request:
    paths:
      - "packages/ethds-react/**"
      - "packages/ethds-tokens/**"
      - ".github/workflows/react.yml"

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      - name: Install
        run: npm ci
      - name: Build tokens (dependency)
        run: npm run build -w @ethds/tokens
      - name: Typecheck
        run: npm run typecheck -w @ethds/react
      - name: Test (Vitest + axe)
        run: npm run test -w @ethds/react
      - name: Build library
        run: npm run build -w @ethds/react
