---
id: login
title: Login
hide_title: true
sidebar_position: 1
---

import Example from '@site/src/components/Example';
import { LoginForm } from '@ethds/patterns';

<Example>
  <LoginForm
    onSubmit={async () => {
      throw new Error('demo');
    }}
    forgotPasswordHref="#"
    registerHref="#"
    onOtpLogin={() => {}}
  />
</Example>

Authenticate a returning citizen — phone-first (the most common
identifier in Ethiopia), with paste and password managers allowed
throughout. Try submitting the form above: this demo always shows the
failure state, so you can see the generic, non-revealing error and the
focus move to it.

```tsx
import { LoginForm } from '@ethds/patterns';

<LoginForm
  onSubmit={async (identifier, password) => {
    /* call your auth API; only reject on genuine failure */
  }}
  forgotPasswordHref="/reset-password"
  registerHref="/register"
  onOtpLogin={() => {
    /* switch to the OTP Verification pattern */
  }}
/>
```

## When to use this pattern

Use Login whenever a citizen needs to access an existing account or
service. Related: [OTP Verification](otp-verification), for a passwordless
"log in with a code" option; [Password Reset](password-reset), linked from
"Forgot your password?"; [Registration](registration), for new citizens.

## Validation & error handling

- **Identifier:** required — an Ethiopian phone (`09xxxxxxxx` /
  `+2519xxxxxxxx`) or an email.
- **Password:** required; no maximum length below 64; paste and password
  managers are never blocked (WCAG 2.2 SC 3.3.8 Accessible
  Authentication).
- **Wrong credentials:** a single generic message — never reveals which
  part was wrong or whether the account exists, preventing account
  enumeration.
- **Rate limiting:** after repeated failures, offer OTP login or password
  reset rather than a hard dead end.

## Accessibility

- The error is a focusable `Alert` (`role="alert"`) that receives focus
  the moment it appears, so assistive technology announces it
  immediately on a failed submit.
- Every field has a visible, programmatically associated label.
- Correct `autocomplete` (`username`, `current-password`) so browsers and
  assistive tools can fill fields.

## Plain HTML

Not on React? Login is arguably simpler without a client framework — a
plain `<form method="post">` the server validates and re-renders with the
same non-revealing error on failure, no client-side state required.

```html
<form method="post" action="/login" novalidate>
  <h1>Log in</h1>

  <!-- Only present when the server re-renders after a failed submit. -->
  <div role="alert" tabindex="-1" class="ethds-alert ethds-alert--error" id="login-error">
    The phone number/email or password is incorrect.
  </div>

  <div class="ethds-field">
    <label for="username" class="ethds-label">Phone number or email</label>
    <input id="username" name="username" type="text" inputmode="tel"
           autocomplete="username" class="ethds-input" required />
  </div>

  <div class="ethds-field">
    <label for="password" class="ethds-label">Password</label>
    <input id="password" name="password" type="password"
           autocomplete="current-password" class="ethds-input" required />
  </div>

  <a href="/reset" class="ethds-link">Forgot your password?</a>
  <button type="submit" class="ethds-button ethds-button--primary">Log in</button>
  <a href="/login/otp" class="ethds-button ethds-button--secondary">Log in with a code</a>
  <p>New here? <a href="/register" class="ethds-link">Create an account</a></p>
</form>

<script>
  // Focus-on-reload replaces the React version's focus-on-submit, since
  // the server already re-rendered the page with the error in place.
  document.getElementById('login-error')?.focus();
</script>
```

Multi-step patterns ([Application Submission](/docs/patterns/application-submission),
Password Reset) are built on a client-side hook in React
(`useMultiStepForm`); without a client framework the natural equivalent
is **multi-page** — one `<form>` per step, posting to the next step's
URL, with the draft held in the server session — not a JavaScript port of
the hook. See the
[pattern's own HTML Example](https://github.com/yidnekachewkibru/ethgov-design-system/blob/main/packages/ethds-patterns/patterns/login.md#html-example)
for the full write-up.

## Localization

All labels, buttons, and the error message are translatable via the
`labels` prop. The identifier field uses `inputMode="tel"` so mobile
keyboards default to numeric entry without blocking email input.

## Props

| Prop | Type | Required |
|---|---|---|
| `onSubmit` | `(identifier: string, password: string) => Promise<void>` | Yes |
| `onOtpLogin` | `() => void` | No — hides the OTP button when omitted |
| `forgotPasswordHref` | `string` | Yes |
| `registerHref` | `string` | Yes |
| `labels` | `Partial<LoginFormLabels>` | No |
